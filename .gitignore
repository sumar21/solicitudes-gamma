node_modules/
dist/
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store

.vercel
.obsidian/

# Reportes de sesión generados (el HTML fuente sí se versiona)
reporte-sesion-*.pdf

# Export de facturación de Vercel (datos de toda la cuenta, no del proyecto)
vercel-costs.csv

# Vercel log exports (pueden traer data sensible de push) — nunca commitear
docs/vercel-logs/

# TestSprite: los intermedios (tmp/config.json, tmp/mcp.log) Y los tests generados
# (testsprite_tests/*.py) traen el password del test account HARDCODEADO. No commitear nada.
testsprite_tests/

# Graphify: grafo + caches (output de la herramienta, se regenera). No commitear.
graphify-out/
